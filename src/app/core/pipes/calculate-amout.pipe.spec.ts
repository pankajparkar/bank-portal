import { CalculateAmoutPipe } from './calculate-amout.pipe';

describe('CalculateAmoutPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateAmoutPipe();
    expect(pipe).toBeTruthy();
  });
});
